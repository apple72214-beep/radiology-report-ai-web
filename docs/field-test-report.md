# محضر اختبار الميدان — Field Test Report
**التاريخ / Date:** 2026-09-14 · **الجهاز / Device:** Android + Chrome (شبكة LTE/واي فاي، اليمن) · **النسخة / Editions:** browser-only (githack mirror) + server edition (Cloudflare temp)

## 1) نتائج مصفوفة الاختبار / Test matrix
| الاختبار / Test | النتيجة / Result | ملاحظات / Notes |
|---|---|---|
| فتح التطبيق الدائم (github.io) | ❌ على واي فاي المستخدم | صفحة بيضاء فارغة = اعتراض/حجب شبكي لنطاق *.github.io تحديدًا؛ يعمل من بيئتنا (200) |
| فتح التطبيق الدائم (raw.githack.com) | ✔ | بعد ضغط زر «Open the page» في بوابة githack |
| تثبيت PWA والعمل Offline | ✔ | مثبّت على الشاشة الرئيسية ويعمل بدون إنترنت |
| توليد ديمو + فرز الحرج أولًا | ✔ | DEMO-0001 حرج / DEMO-0002 روتيني |
| تنزيل نماذج .dcm عبر jsdelivr | ✔ | روابط دائمة: cdn.jsdelivr.net/gh/.../samples/{lesion,clean}.dcm |
| رفع .dcm مفرد (lesion/clean) | ✔ | ظهر SYN-LESION حرج وSYN-CLEAN روتيني بالترتيب الصحيح |
| العارض + نوافذ (رئة/منصف/عظم) | ✔ | الآفة الاصطناعية واضحة تحت نافذة رئة |
| منزلق الشرائح | ✔ (مشروط) | يتحرك فقط مع دراسات متعددة الشرائح (DEMO-0001) |
| رفع ZIP حاوٍ على DICOM | ✔ جزئي | فك الأرشيف داخل المتصفح يعمل؛ الأعضاء غير المضغوطة تُضاف، المضغوطة تُرفض |
| رسائل الحالة بالعربية | ✔ | وصلت في v2 (جملة الرفض الحمراء ظهرت صحيحة) |
| DICOM مضغوط JPEG2000/JPEG-LS | ❌ | يُرفض بصدق مع رسالة واضحة — فك الضغط = v0.3b |
| MR قيم سالبة/Rescale | ❌ ثم ✔ | كان شاشة سوداء؛ أصلحه v4 (Int16 + RescaleSlope/Intercept) |
| مسودة التقرير ع/EN + الاعتماد | ✔ | v3/v4: فقرتا ع/EN بالانطباع الربعي والمؤشر، اعتماد باسم «Dr abdullah Hagar» + زر نسخ — مُتحقق على الجهاز 05:44 |
| عرض MR حقيقي (قيم سالبة/Rescale) | ✔ | v4: صورة دماغ محورية واضحة بدل الشاشة السوداء |
| حكم الـ zip النهائي | ✔ صادق | الأرشيف مضغوط بالكامل (JPEG2000/JPEG-LS) ⇒ رفض أحمر صريح؛ فك الضغط = مهمة v0.3b الأولى |

## 2) دروس البنية التحتية / Infrastructure lessons
- **hosting المؤقت (trycloudflare) يموت مع كل تصفير للبيئة** — لا يصلح رابطًا للمستخدم؛ الاعتماد النهائي على مستودع عام + مرايا (githack للدخول، jsdelivr للتنزيلات).
- **مرايا CDN تخزّن الفروع مؤقتًا** (~10 دقائق لوحظت على githack؛ أطول على jsdelivr للملفات الموجودة مسبقًا) ⇒ بعد كل دفع: انتظار ~10 دقائق قبل فتح التطبيق؛ المسارات الجديدة تصل فورًا.
- **مسح بيانات الموقع على الجهاز يمسح دراسات IndexedDB** — لا يُنصح به بعد أول استخدام؛ التحديث يأتي عبر تدفق sw.js تلقائيًا.
- **حجب *.github.io شبكيًا عند المستخدم** بينما github.com وraw.githack.com يعملان ⇒ التوزيع متعدد المرايا ضرورة وليس رفاهية.

## 3) الفجوات المفتوحة → v0.3b / Open gaps
1. فك ضغط JPEG2000/JPEG-LS/RLE داخل المتصفح (ملفات الأقسام الحقيقية غالبًا مضغوطة).
2. إدارة القائمة: حذف دراسة/تفريغ الكل — منفذة في v5 (زر ✕ لكل صف + زر تفريغ بتأكيد)، بانتظار الدفع.
3. العارض: تقريب/سحب/قياس مسافات، ومزامنة نافذة بين الشرائح.
4. دفع تحديثات المرايا: توثيق نافذة الـ10 دقائق في إرشادات المستخدم.
5. ترقية VLM (MedGemma) لمسودة التقرير = v0.3b-متأخر عند توفر موارد.

## 4) قرار الاعتماد / Sign-off principle
كل مخرجات v0.3a تحمل سطري: «ليست تشخيصًا» و«مراجعة الأخصائي إلزامية» — لا يُصدر التقرير النهائي إلا باعتماد بشري مسجل باسم الأخصائي (signedBy).

## ملحق 2026-09-15 (ساعة الليل): حرب النطاقات على شبكة LTE اليمن
- الأعراض المتسلسلة: تعليق github.io (1:28) ← ERR_FAILED على raw.githack (1:52) ← rawcdn فتح مرة (1:55) ثم ERR_FAILED (1:56+) ← raw.githubusercontent ميت ← dns.google DoT محجوب (منفذ 853 مسدود: «تعذّر الاتصال» لكل الشبكة) ← 2:26 كل البوابات ميتة بينما github.com وgoogle.com يعملان.
- الاستنتاج: اعتراض على مستوى المزوّد (SNI/حافة) لعائلة github.io + githack + raw.githubusercontent، وليس DNS فقط (DoT نفسه محجوب). الخنق ليس مؤقتًا حتى الآن (>30 دقيقة).
- ما نجا: github.com، google.com، jsdelivr (text/plain + nosniff ⇒ لا يصلح وثيقة عليا)، facebook/youtube.
- الهندسة المضادة المنشورة: v12 (نسخ المخزن المؤقت عند الاستقبال + قياس رسم= + مسار مباشر ذاتي التبني) وv13 (تثبيت SW متسامح مع النقص + قاذفة تجلب الأصول من 4 أصول: محلي/jsdelivr/rawcdn/raw.githack).
- خطة الصباح: (ب) أول فتح ناجح على أي شبكة بديلة (واي فاي مستشفى/منزل/شريحة أخرى) يثبّت الصدفة للأبد عبر SW v13؛ (أ) احتياط: بوتستراب Blogger (نطاق google 도달) يحمّل الأصول من jsdelivr.
